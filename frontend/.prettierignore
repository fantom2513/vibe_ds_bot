dist
node_modules
playwright-report
test-results
blob-report
package-lock.json

# Публичный лендинг — самодостаточная страница со своими вендорными
# библиотеками (GSAP), а не исходники React-приложения. Минифицированные
# бандлы форматировать бессмысленно, а index.html написан плотно: Prettier
# раздувает его со 144 строк до 2122, ничего не улучшая. Смысл общего
# форматтера — единый стиль внутри кодовой базы приложения, и отдельный
# артефакт под это правило не подпадает.
public/landing
