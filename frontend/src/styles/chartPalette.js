// Stable, chart-only categorical series colors. These distinguish people
// inside charts (assigned by member order, not rank, so the palette stays
// stable as the tracked-member list changes) and are explicitly
// non-semantic — never reuse them for buttons, badges, alerts, or status
// indicators. Semantic tone lives in StatusBadge / theme.js instead.
export const chartSeriesColors = [
  '#65C69C',
  '#67B9DE',
  '#DDB868',
  '#E58A94',
  '#8FC8B1',
  '#82AFC7',
  '#C8A985',
  '#A8B5B0',
]
