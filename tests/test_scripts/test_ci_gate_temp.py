"""
ВРЕМЕННЫЙ файл. Проверяет, что упавший тест доводит ci-ok до красного.

Удаляется сразу после того, как прогон это подтвердит. Смысл проверки в
том, что гейт, который ни разу не краснел, не является гейтом — он просто
выглядит как защита.
"""


def test_deliberate_failure_to_verify_the_ci_gate():
    assert 1 == 2, "умышленное падение: проверяем, что ci-ok краснеет"
